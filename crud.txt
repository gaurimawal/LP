server.js

const express = require('express');
const mongoose = require('mongoose');
const bodyParser = require('body-parser');

const app = express();

// Middleware
app.use(bodyParser.json());

// MongoDB Connection
mongoose.connect('mongodb://127.0.0.1:27017/studentDB')
.then(() => console.log('MongoDB Connected'));

// Schema
const studentSchema = new mongoose.Schema({
    name: String,
    age: Number,
    course: String
});

// Model
const Student = mongoose.model('Student', studentSchema);


// ---------------- CREATE API ----------------
app.post('/addStudent', async (req, res) => {

    const student = new Student(req.body);
    await student.save();

    res.json({
        message: 'Student Added Successfully',
        data: student
    });

});


// ---------------- READ API ----------------
app.get('/getStudents', async (req, res) => {

    const students = await Student.find();

    res.json({
        message: 'Students Retrieved Successfully',
        data: students
    });

});


// ---------------- UPDATE API ----------------
app.put('/updateStudent/:id', async (req, res) => {

    const updatedStudent = await Student.findByIdAndUpdate(
        req.params.id,
        req.body,
        { new: true }
    );

    res.json({
        message: 'Student Updated Successfully',
        data: updatedStudent
    });

});


// ---------------- DELETE API ----------------
app.delete('/deleteStudent/:id', async (req, res) => {

    await Student.findByIdAndDelete(req.params.id);

    res.json({
        message: 'Student Deleted Successfully'
    });

});


// Server
const PORT = 3000;

app.listen(PORT, () => {
    console.log(`Server Running on Port ${PORT}`);
});




package.json

{
  "name": "crud-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "body-parser": "^2.2.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.1",
    "nodemon": "^3.1.14"
  }
}



==================================================================================
Create 4 APIs using Node.js, Express.js, and MongoDB for CRUD Operations using Postman and MongoDB Compass.

Software Required
Node.js
Postman
MongoDB Compass
Step 1: Create Project Folder

Open terminal and run:

mkdir crud-api
cd crud-api
Step 2: Initialize Node Project
npm init -y
Step 3: Install Packages
npm install express mongoose body-parser nodemon
Step 4: Create File Structure

Create files:

touch server.js

Folder structure:

crud-api
│
├── server.js
├── package.json
Step 5: Start MongoDB Compass
Open MongoDB Compass
Connect using:
mongodb://localhost:27017
Database will automatically create after running API.
Step 6: Write Code in server.js
const express = require('express');
const mongoose = require('mongoose');
const bodyParser = require('body-parser');

const app = express();

// Middleware
app.use(bodyParser.json());

// MongoDB Connection
mongoose.connect('mongodb://127.0.0.1:27017/studentDB')
.then(() => console.log('MongoDB Connected'))
.catch((err) => console.log(err));

// Schema
const studentSchema = new mongoose.Schema({
    name: String,
    age: Number,
    course: String
});

// Model
const Student = mongoose.model('Student', studentSchema);


// ---------------- CREATE API ----------------
app.post('/addStudent', async (req, res) => {
    try {
        const student = new Student(req.body);
        await student.save();

        res.status(201).json({
            message: 'Student Added Successfully',
            data: student
        });

    } catch (error) {
        res.status(500).json({
            message: 'Error Adding Student'
        });
    }
});


// ---------------- READ API ----------------
app.get('/getStudents', async (req, res) => {
    try {
        const students = await Student.find();

        res.status(200).json({
            message: 'Students Retrieved Successfully',
            data: students
        });

    } catch (error) {
        res.status(500).json({
            message: 'Error Fetching Students'
        });
    }
});


// ---------------- UPDATE API ----------------
app.put('/updateStudent/:id', async (req, res) => {
    try {

        const updatedStudent = await Student.findByIdAndUpdate(
            req.params.id,
            req.body,
            { new: true }
        );

        res.status(200).json({
            message: 'Student Updated Successfully',
            data: updatedStudent
        });

    } catch (error) {
        res.status(500).json({
            message: 'Error Updating Student'
        });
    }
});


// ---------------- DELETE API ----------------
app.delete('/deleteStudent/:id', async (req, res) => {
    try {

        await Student.findByIdAndDelete(req.params.id);

        res.status(200).json({
            message: 'Student Deleted Successfully'
        });

    } catch (error) {
        res.status(500).json({
            message: 'Error Deleting Student'
        });
    }
});


// Server
const PORT = 3000;

app.listen(PORT, () => {
    console.log(`Server Running on Port ${PORT}`);
});
Step 7: Run Server

Add script in package.json:

"scripts": {
  "start": "nodemon server.js"
}

Run command:

npm start

Output:

MongoDB Connected
Server Running on Port 3000
Step 8: Test APIs in Postman
1. CREATE API

Method: POST

URL:

http://localhost:3000/addStudent

Body → raw → JSON

{
    "name": "Rahul",
    "age": 21,
    "course": "MCA"
}

Response:

{
    "message": "Student Added Successfully"
}
2. READ API

Method: GET

URL:

http://localhost:3000/getStudents

Response:

{
    "message": "Students Retrieved Successfully"
}
3. UPDATE API

Method: PUT

URL:

http://localhost:3000/updateStudent/ID

Replace ID with MongoDB document id.

Body:

{
    "course": "BCA"
}

Response:

{
    "message": "Student Updated Successfully"
}
4. DELETE API

Method: DELETE

URL:

http://localhost:3000/deleteStudent/ID

Response:

{
    "message": "Student Deleted Successfully"
}
Check Data in MongoDB Compass
Open MongoDB Compass
Refresh database
Open:
studentDB
students collection
View inserted data
Viva Questions
1. What is CRUD?

CRUD stands for:

Create
Read
Update
Delete
2. What is Express.js?

Express.js is a Node.js framework used to create APIs and web servers.

3. What is MongoDB?

MongoDB is a NoSQL database that stores data in JSON-like documents.

4. What is Mongoose?

Mongoose is an ODM library used to connect Node.js with MongoDB.

5. What is Postman?

Postman is an API testing tool.

6. Difference between PUT and POST?
POST → Insert data
PUT → Update data
7. Which HTTP methods are used in CRUD?
POST → Create
GET → Read
PUT → Update
DELETE → Delete
8. What is Middleware?

Middleware processes request before sending response.

9. What is Schema?

Schema defines structure of MongoDB document.

10. Default Port Used?

Port 3000 is used in this program.

